apiVersion: v1
kind: Service
metadata:
  name: notes-backend
  labels:
    app: notes-backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: notes-backend

